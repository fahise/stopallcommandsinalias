So our first command is using alias
    alias "a" """echo" "sa"; "alias" "annen" "clear"""
    
    we're saying here if user says a, go say "sa" and at the same time if we write "annen" it stops command.
    
